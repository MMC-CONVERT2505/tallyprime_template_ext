# Backend API base URL, including the /api prefix. Must be reachable from the browser
# (the backend has CORS enabled by default — see main.ts's app.enableCors()).
VITE_API_BASE_URL=http://localhost:3000/api
